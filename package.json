{
  "type": "module",
  "scripts": {
    "test": "NODE_ENV=${NODE_ENV:-development} NODE_OPTIONS=--experimental-vm-modules npx @dotenvx/dotenvx run --env-file .env.local -- npx jest"
  },
  "dependencies": {
    "fets": "^0.8.1",
    "reflect-metadata": "^0.2.2",
    "tsyringe": "^4.8.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/lodash.merge": "^4.6.9",
    "lodash.merge": "^4.6.2",
    "ts-jest": "^29.2.3",
    "ts-node": "^10.9.2"
  }
}
